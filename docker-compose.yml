version: '3.0'
services:
  backend:
    build: .
    ports:
      - "8555:8555"
    container_name: inventaryAPI
    # environment:

    volumes:
      - .:/code
    restart: always
    networks:
      - back
    # depends_on:
    #   - db
networks:
    back:

